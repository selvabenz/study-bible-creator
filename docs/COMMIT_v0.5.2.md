# Git commit plan — v0.5.2

Recommended commit:

`fix(desktop): repair Windows GUI startup and local-engine bootstrap`

Optional split history:

- `fix(desktop): use Windows GUI subsystem for release builds`
- `fix(desktop): add startup window health gate and diagnostics`
- `test(desktop): cover Windows runtime bootstrap regression`
- `docs: add v0.5.2 runtime hotfix notes`

Tag only after the Desktop Installer Smoke Build succeeds:

`v0.5.2`
