# GitHub-ready commit plan — v0.4.0

Recommended logical commits:

```text
feat(import): add canonical match identities and authority-aware conflict preflight
feat(conflicts): add auditable human conflict resolution and protected Scripture confirmation
feat(export): add rigorous JSON CSV TSV SFM and DOCX round-trip exchange
feat(qa): expand deterministic Unicode USFM anchor and bilingual structure checks
feat(ux): add visual conflict comparison and authority review workspace
test: add v0.4 edge-case round-trip authority and Matthew corpus gates
ci(release): add tag-version validation release notes checksums and conditional desktop packaging
docs: document v0.4 release process tests and import authority policy
```

Suggested release tag:

```text
v0.4.0
```

Release title:

```text
Study Bible Creator v0.4.0 — Import & Conflict Resolution Hardening
```
