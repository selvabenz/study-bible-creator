# Suggested Git commit — v0.5.5

```text
fix(desktop): harden local engine startup and installed runtime tests
```

Then push `main` and run **Desktop Installer Smoke Build**. Do not create the `v0.5.5` tag until Windows, macOS and Linux smoke jobs are green.
