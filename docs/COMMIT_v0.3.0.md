# GitHub-ready commit plan — v0.3.0

Recommended commits:

```text
feat(core): add semantic source-target pairing and persistent import conflicts
feat(qa): add deterministic Unicode number and versification checks
feat(ui): connect publishing workspace to the local Study Bible database
fix(import): preserve original filenames and bilingual Word language roles
fix(qa): normalize reference ranges and numeric ordering to reduce false positives
test: add v0.3 regression and real Matthew smoke coverage
docs: document v0.3 capabilities limitations and test results
```

Suggested release tag:

```text
v0.3.0
```
